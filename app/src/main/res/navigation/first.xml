<?xml version="1.0" encoding="utf-8"?>

<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home"
    app:startDestination="@+id/friendListScreen">

    <fragment
        android:id="@+id/friendListScreen"
        android:name="com.sample.chatapp4.firstscreen.FriendListFragment"
        android:label="친구"
        tools:layout="@layout/fragment_friend_list">
        <action
            android:id="@+id/action_friendListScreen_to_friendProfileScreen"
            app:destination="@id/friendProfileScreen" />
    </fragment>
    <fragment
        android:id="@+id/friendProfileScreen"
        android:name="com.sample.chatapp4.firstscreen.FriendProfileFragment"
        android:label=""
        tools:layout="@layout/fragment_friend_profile">
        <deepLink
            android:id="@+id/deepLink"
            app:uri="www.example.com/user/{userName}"
            android:autoVerify="true"/>
        <argument
            android:name="userName"
            app:argType="string"/>
    </fragment>
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.sample.chatapp4.firstscreen.SearchFragment"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search" />
</navigation>
